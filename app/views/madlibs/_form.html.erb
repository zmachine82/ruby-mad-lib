<%= form_with(model: madlib) do |form| %>
  <% if madlib.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(madlib.errors.count, "error") %> prohibited this madlib from being saved:</h2>

      <ul>
        <% madlib.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :template, style: "display: block" %>
    <%= form.text_area :template %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
